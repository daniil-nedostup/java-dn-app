apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-dn-app.fullname" . }}
  labels:
    {{- include "java-dn-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-dn-app.selectorLabels" . | nindent 4 }}
